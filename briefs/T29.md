# T29 — App-two: fit the standings ("Table") to the screen width (no horizontal scroll)

**Branch:** `task/T29-standings-fit` (cut from **current `main`**; verify base with `git merge-base --is-ancestor main HEAD`). *Independent of T28* — T28 touches `member_auth_screen.dart`, this touches `matches_screen.dart`, so branch from whatever `main` is when you start (before or after T28 lands is fine). One task, one branch. **Commit everything before handoff** (clean worktree). **Mobile-only, additive — no backend/PHP changes** (`php artisan test` untouched). **App one behaviour-identical** (`matches_screen.dart` is app-two only). Flutter **3.44.4**; no `flutter create`.

**UAT:** the Matches → **Table** (standings) view is wrapped in a horizontal `SingleChildScrollView` with `ConstrainedBox(minWidth: 640)` and fixed-pixel `_TableCell` widths, so on a phone you must **swipe sideways** to see W/D/L/GD/Pts. Make the full table **fit the screen width** — no horizontal scroll — while keeping all columns.

## Fix — make the table width-responsive (`matches_screen.dart`)
1. **`_StandingsView`:** drop the horizontal `SingleChildScrollView` + `ConstrainedBox(minWidth: 640)`. Render `_StandingsTable(rows: rows)` **directly, full-width**, still inside the vertical `ListView` + `RefreshIndicator` (keep pull-to-refresh). The table now sizes to the available width.
2. **`_TableCell`:** replace the fixed `double width` with a **flex weight** and wrap each cell in `Expanded(flex: …)` so the columns share the row width. Keep the rest (`header`, `isOwnClub`, `alignStart`, `emphasize`, the `own-club-cell` key logic, centre-align numerics / start-align club, `TextOverflow.ellipsis`).
3. **Column weights** — club flexible, numeric columns narrow + equal; tune so all 8 columns fit a ~360dp-wide phone without overflow. Suggested starting weights: **Pos 3, Club 9, P 3, W 3, D 3, L 3, GD 4, Pts 4** (apply the same weights in `_StandingsHeader` and `_StandingsRow`). Adjust if the longest club name ("Al Shahania") or a "-18" GD clips.
4. **Compactness** — trim the cell font a touch (numerics ~12–13, keep `Pts` emphasized) and reduce row vertical padding (e.g. ~10) so the 13 rows sit comfortably and vertical scrolling is minimal. Keep the header tint, zebra rows, the **gold `BorderDirectional` own-club left-accent + tint**, and RTL correctness (`EdgeInsetsDirectional`).
5. **Preserve** the widget keys/behaviour tests rely on: `Key('standing-row-${row.position}')` on each row and `Key('own-club-cell')` on the own-club club-name cell. Keep `_StandingsTable` building **all rows in a `Column`** (not lazy) so every row stays in the tree.

## Tests & verification (run here)
- `flutter analyze` clean + `flutter test` green in **`lfc_core`**, **`demo_app_one`** (regression), **`demo_app_two`**. The existing `supporter_app_test` standings test (taps **Table**, expects `own-club-cell`, `standing-row-1`, and the club-name texts) must **stay green** unchanged.
- Add/extend a widget test asserting the standings view **no longer contains a horizontally-scrolling viewport** (e.g. no `Scrollable` with `Axis.horizontal` under the Table tab) — i.e. the table fits width. Respect the 800×600 test viewport + finder gotchas ([[mobile-design-system]]); at 800 wide the flex table lays out fine.
- On-device (user; note pending): the league table shows all 8 columns within the phone width with **no sideways scroll**, Lusail row still highlighted, EN + AR/RTL intact.
- `php artisan test` untouched/green.

## Scope / hygiene
- One view in one file (`matches_screen.dart` standings widgets). Don't touch fixtures/results cards, the gate, app one, or the backend. Keep all columns (the user wants the full table, just fitted).
- Branch from current `main`; verify base; commit the whole task before handoff.
- ≤6-line handoff summary: removed the horizontal scroll + fixed widths, made `_TableCell` flex-based to fit width, the weights used, compactness tweaks, keys/own-club highlight preserved, and `flutter analyze`/`test` results across all three packages.
