# T27 — App-two content refresh: official Lusail news+photos, real QSL standings, Western digits

**Branch:** `task/T27-official-content` (cut from current `main` = T26 `6a078a5`; verify base with `git merge-base --is-ancestor main HEAD`). One task, one branch. **Commit everything before handoff** (clean worktree). **Do NOT run `flutter create`/`shield:generate`.** Reference toolchain **Flutter 3.44.4**. Spans **mobile + backend + seed + downloaded image assets + docs + tests** — run **`php artisan storage:link`**, **`php artisan migrate:fresh --seed`** (clean), **`php artisan test`** (green), and `flutter analyze`/`flutter test` (green in `lfc_core` + both apps) before committing. **App one stays behaviour-identical.**

Three user-requested changes from the UAT pass, all **app-two only**:
1. **Western digits everywhere in app two** (revert the Arabic-Indic score; make app-two dates Western too).
2. **Real Lusail SC news + official photos** (downloaded from the club site, served by the demo server).
3. **Real QSL Doha-Bank-Stars-League standings**, with Lusail SC inserted as the highlighted own-club (decisions locked with the user).

---

## Fix 1 — Western digits (app-two only; do NOT touch app one)
`AppDateFormat` is **shared** with app-one screens (`offers`, `rewards`, `points_history`, `staff_scanner`) which must keep locale-driven (Arabic-Indic) digits — so **don't change the existing `date()`/`dateTime()`**. Instead:
- **`core/formatting/app_date_format.dart`:** add `static DateFormat westernDate() => DateFormat('dd-MM-yyyy', 'en');` and `static DateFormat westernDateTime() => DateFormat('dd-MM-yyyy HH:mm', 'en');` (the `'en'` locale forces Latin digits; the pattern is purely numeric so it reads fine in AR/RTL).
- Switch the **app-two screens** from `AppDateFormat.date(locale)`/`dateTime(locale)` to the western variants: `content/presentation/home_news_screen.dart`, `news_detail_screen.dart`, `matches_screen.dart` (both the fixture `dateTime` and result `date`), and `membership/presentation/vvip_card_screen.dart` (valid-until + the two offer dates). (The discount wallet already formats Western manually — leave it.)
- **`matches_screen.dart` `_ResultCard`:** delete the `_localizedScore` helper and render the score with plain Western digits: `'${match.ourScore ?? 0}–${match.opponentScore ?? 0}'`. Keep the RTL-safe `Row` layout + en-dash.

## Fix 2 — Real Lusail news + official photos (bilingual, served locally)
**Download the official images** (verified reachable, HTTP 200 `image/jpeg`) into the public disk and commit them:
```bash
php artisan storage:link   # public/storage symlink is currently MISSING
mkdir -p storage/app/public/news
curl -fsSL https://www.lusailsc.qa/assets/images/news1.jpg -o storage/app/public/news/lusail-news-1.jpg
curl -fsSL https://www.lusailsc.qa/assets/images/news2.jpg -o storage/app/public/news/lusail-news-2.jpg
curl -fsSL https://www.lusailsc.qa/assets/images/news3.jpg -o storage/app/public/news/lusail-news-3.jpg
```
Commit the three JPEGs (they're ~128–470 KB each) and **do not `.gitignore` them**. (Confirm `git status` shows them; `storage/app/public/**` may be gitignored — add them with `git add -f` if so, or place under `public/images/news/` instead and adjust the path below. Prefer whichever the repo already serves.)

**`database/seeders/AppTwoDemoSeeder.php`:** replace the four invented news posts with **three real Lusail SC items** (bilingual, `is_published => true`, staggered `published_at`), each `image_path => 'news/lusail-news-N.jpg'` (the storage path, matching where you saved them):
1. `title` "Eyyal Al-Freej community tournament concludes" / `title_ar` "ختام بطولة عيال الفريج" — excerpt EN "Lusail SC wrapped up its Eyyal Al-Freej community tournament under the patronage of the Ministry of Sports and Youth." / AR "اختتم نادي لوسيل بطولة عيال الفريج المجتمعية تحت رعاية وزارة الرياضة والشباب." (image `lusail-news-1.jpg`)
2. `title` "Lusail held to a draw by Al-Bedaa in the second-division finale" / `title_ar` "نادينا يتعادل مع نادي البدع الرياضي" — excerpt EN "Our team drew with Al-Bedaa SC in the final round of the Qatari Second Division." / AR "تعادل فريقنا أمام نادي البدع الرياضي ضمن منافسات الجولة الأخيرة لدوري الدرجة الثانية." (image `lusail-news-2.jpg`)
3. `title` "Club president honoured with the Sports & Youth Excellence Award" / `title_ar` "رئيس نادي لوسيل يحصل على جائزة التميز الرياضي والشبابي" — excerpt EN "Lusail SC president Nawaf Mohammed Al-Mudahka received the Sports and Youth Excellence Award on behalf of the Sumaismah Youth Centre." / AR "حصل رئيس نادي لوسيل نواف محمد المضاحكة على جائزة التميز الرياضي والشبابي ممثلاً عن مركز شباب سميسمة." (image `lusail-news-3.jpg`)
Write a fuller `body`/`body_ar` (2–3 sentences) faithful to each headline. Keep everything idempotent (key on `title`). (≥3 published posts keeps `AppTwoDemoSeederInvariantsTest` green.)

**Serve them so the mobile can load them on any platform.** `Storage::url()` here returns an **APP_URL-absolute** `http://localhost/storage/...`, which fails on the Android emulator (`10.0.2.2`) and physical devices. So:
- **`app/Http/Controllers/Api/V1/NewsController.php`:** for a stored image return a **root-relative** path, not the absolute one — full external URLs still pass through:
  `image_url => Str::startsWith($post->image_path, ['http://','https://']) ? $post->image_path : '/'.ltrim(parse_url(Storage::url($post->image_path), PHP_URL_PATH) ?? '', '/')` → yields `/storage/news/lusail-news-1.jpg`.
- **Mobile:** add a small resolver (e.g. `content/util/media_url.dart`): given the raw `image_url` and the API base URL, return the raw value if it starts with `http://`/`https://`, else join it onto the **origin** (`scheme://host:port`) of `config.apiBaseUrl`. Use it in `home_news_screen.dart` and `news_detail_screen.dart` where `Image.network(article.imageUrl!)` is called (read `appConfigProvider` for the base). Keep the existing `errorBuilder`. This makes `/storage/...` resolve against whatever host the app already uses for the API.

## Fix 3 — Real QSL standings + Lusail inserted (locked)
Replace the seeded `standings` with the **real QSL Doha-Bank-Stars-League table** (Position + Played + Points are the real published values; W/D/L/GF/GA are filled to match the real points since QSL doesn't publish them) **plus Lusail SC inserted as the highlighted own-club at position 8**. Seed these **13 rows** in order (all `played => 22`, bilingual `club_name`/`club_name_ar`, exactly one `is_own_club => true`):

| Pos | club_name | club_name_ar | W | D | L | GF | GA | Pts | own |
|----|----|----|----|----|----|----|----|----|----|
| 1 | Al Sadd | السد | 14 | 3 | 5 | 43 | 18 | 45 | |
| 2 | Al Shamal | الشمال | 12 | 4 | 6 | 35 | 22 | 40 | |
| 3 | Al Rayyan | الريان | 11 | 5 | 6 | 34 | 24 | 38 | |
| 4 | Al Gharafa | الغرافة | 10 | 6 | 6 | 32 | 25 | 36 | |
| 5 | Al Duhail | الدحيل | 9 | 6 | 7 | 33 | 27 | 33 | |
| 6 | Qatar SC | نادي قطر | 9 | 5 | 8 | 29 | 26 | 32 | |
| 7 | Al Arabi | العربي | 8 | 8 | 6 | 28 | 26 | 32 | |
| 8 | **Lusail SC** | **نادي لوسيل** | 8 | 6 | 8 | 27 | 27 | 30 | **✓** |
| 9 | Al Wakrah | الوكرة | 7 | 6 | 9 | 25 | 30 | 27 | |
| 10 | Al Ahli | الأهلي | 7 | 5 | 10 | 24 | 31 | 26 | |
| 11 | Al Sailiya | السيلية | 5 | 7 | 10 | 20 | 33 | 22 | |
| 12 | Al Shahania | الشحانية | 5 | 6 | 11 | 19 | 34 | 21 | |
| 13 | Umm Salal | أم صلال | 5 | 5 | 12 | 18 | 36 | 20 | |
(Each row is internally consistent: `W+D+L=22`, `3·W+D=Pts`, and GD descends monotonically.) `goal_difference` is computed by the model — don't store it. **Every row still explicitly `app => AppKey::AppTwo->value`** (the `WithoutModelEvents` footgun). Keep the seed idempotent (key on `club_name`). **Leave the fixtures/results as they are** (Lusail vs real QSL clubs — already bilingual from T26); the `AppScopingTest` fixtures-count (8) must stay unchanged.

## Setup / walkthrough
`docs/DEMO-WALKTHROUGH.md`: add **`php artisan storage:link`** to the app-two setup step (needed for the news photos to serve). Update the seeded-numbers note: standings now **13 rows (Lusail 8th)**, news now the **3 official items**. (Optionally have `AppTwoDemoSeeder` ensure the `public/storage` symlink exists if missing, guarded — but documenting it is enough.)

## Tests & verification (run here — I re-run all of it independently)
- **Backend:** `php artisan storage:link` + `migrate:fresh --seed` clean + idempotent; `php artisan test` green. Update the T26 **news image passthrough** test so the **storage-path** case now expects the **root-relative** `'/storage/news/example.jpg'` (the full-URL case is unchanged). Keep `AppTwoDemoSeederInvariantsTest` green (still `is_own_club == 1`, `>=3` news); `AppScopingTest` fixtures-count (8) untouched. Add an assertion that a seeded news post's `image_url` is root-relative (starts with `/storage/`).
- **Mobile:** `flutter analyze` clean, `flutter test` green in `lfc_core` + `demo_app_one` (regression) + `demo_app_two`. **Update** the T26 AR result test to expect the **Western** score `2–1` (not `٢–١`). Add tests: the **media-url resolver** (`http(s)://` passes through; `/storage/x.jpg` joins onto the API base origin; app-two base host is respected), and (optional) an AR date renders Western digits. Preserve the `own-club-cell`/`standing-row-*` standings assertions.
- On-device (user; note pending): app-two Home shows the real Lusail photos; all digits Western in EN + AR; the table shows the real QSL clubs with Lusail highlighted 8th; news reads in AR/EN.

## Scope / hygiene
- **App-two only.** Don't change app-one screens or the shared `AppDateFormat.date/dateTime` (add the western variants alongside). No `shield:generate`, no policies. Keep the fixtures/results + all other app-two data from T26 intact.
- Every new app-scoped seed row keeps its explicit `app => AppKey::AppTwo->value`. Commit the three downloaded JPEGs. Branch from current `main`; verify base; commit the whole task before handoff.
- ≤10-line handoff summary: the western-digit helpers + app-two screen switch + result-card score; the three official news items + downloaded/served images + NewsController root-relative URL + mobile resolver; the 13-row real-QSL standings with Lusail 8th; the `storage:link` + walkthrough note; the updated/added tests; and `storage:link`+`migrate:fresh --seed`/`php artisan test`/`flutter analyze`+`test` results.
