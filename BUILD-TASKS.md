# Fast-Track Demo — Build Backlog

**Roles:** PM + code review = Claude (Opus, this harness). Implementation = **external opencode agent on DeepSeek**, run by the user on this same repo. (Claude's in-harness subagents are not used — they only run Claude models.)
**Spec:** `implementation-plan.md` — §4 (Phase A), §6 (foundation slice), §7 (Phase D), §8 (demo cuts — authoritative scope).
**Demo rules (§8):** seeded data, TestFlight/APK (no store review), relaxed anti-fraud/offline, **AR + EN / RTL in scope**.

**Loop:** PM expands the next task into a brief → user runs opencode on it → coder implements on a branch + commits → PM reviews diff (`/code-review`), updates status here, writes feedback → user feeds feedback to opencode → repeat. One task per branch; PM never generates the code (keeps Claude's context to diffs only).

**Git hygiene (post-T4 incident):** opencode and Claude share one working dir + HEAD. Before ANY merge/marker-commit, run `git switch main` and verify `git branch --show-current` — never assume. After each merge, assert the prior task's commit is an ancestor of `main`. Coder must branch each `task/T<n>` from current `main` (verify base).

**Status:** TODO / IN PROGRESS / IN REVIEW / DONE

## Backlog

| ID | Status | Task |
| --- | --- | --- |
| T1 | DONE | Scaffold: Laravel + Filament v5, auth, Shield roles (Admin/Coach/Mgmt), `.env`/staging, seeder skeleton |
| T2 | DONE | Phase A core (demo): Candidate resource + teams + seasons; multi-dimension status + transition guards; private-disk documents + consent; mark accepted candidate as "player" |
| T3 | DONE | Accounts/API slice: `parent_accounts`, parent↔player links, invitation; Sanctum API (auth + endpoints D needs) |
| T4 | DONE | Matches/Fixtures module (Filament) with open-for-scanning window |
| T5 | DONE | Points ledger (append-only) + earning-rules engine (fixed/percentage, scoped) |
| T6 | DONE | Attendance scan: rotating signed QR (app) + staff scanner endpoint; validate signature/freshness/open-match; one-scan dedupe; credit linked player(s) on match team |
| T7 | DONE | Redemption catalog (fees/events/merch) + redeem→voucher; VVIP flag + offers (all / VVIP-only) |
| T7b | DONE | VVIP client accounts (non-parent): `account_type` (parent\|vvip_client) on accounts; admin "Create VVIP client" + "Grant points"; account-level points ledger owner + account-balance redemption. Keep the existing per-player path unchanged. |
| T8 | DONE | Loyalty dashboard (Filament): issued/redeemed, attendance, fulfillment, outstanding-points liability |
| T9a | DONE | Flutter scaffold (`mobile/`, iOS+Android) + **AR+EN/RTL** localization + auth/API wiring (Dio client, secure-storage token, session/routing, login + accept-invite, `/me` shell). No feature screens. |
| T9b | DONE | Flutter feature screens (**AR+EN/RTL**): players+balances, show rotating QR, points history, redeem (player or account), offers, staff scanner. Needs `points_balance` added to `PlayerResource`. |
| T11 | DONE | Post-UAT polish (3 device-test bugs): (1) header "Account balance" card shows `0` for parents (account ledger is empty by design — points live on players); show the parent's **players-total** (label `totalPointsLabel`) and keep account balance only for `vvip_client`. (2) redeem "insufficient points" (422) shows the generic "reward unavailable" message; add an `'insufficient'` branch in `rewards_screen._friendlyError` → new `redeemInsufficientError` string (EN+AR). (3) hide the VVIP status chip in the home header when `!account.isVvip` (a "VVIP: No" chip on a parent is noise). |
| T10 | DONE | Seed demo data + scripted end-to-end walkthrough (parent → scan → points → redeem → VVIP offer). **Folded in 3 fixes:** (A) platform-aware API base URL default (Android→10.0.2.2), (B) `ApiException.toString()=>message` (6 screens showed "Instance of 'ApiException'"), (C) **balances went stale after redeem** — Players tab (IndexedStack, fetched once in initState) + profile header (`session.account` login snapshot); now invalidate a shared players provider + `SessionController.refreshAccount()` after redeem/scan. |
| T12a | DONE | **Admin bilingual foundation.** Filament panel AR + RTL + an in-panel EN/AR switcher (persist per session/user). Translate ALL admin UI strings via `lang/{en,ar}` (nav group/item labels, resource labels, table column headers, action labels, form field labels, widget headings). Refactor all 13 `App\Enums\*` `getLabel()`s to `__('enums.<enum>.<case>')` backed by `lang/{en,ar}/enums.php` (labels only — leave `getColor()` alone). No schema/API/mobile changes. |
| T12b | DONE | **Dual-language content storage (app-facing free text only).** Migrations: add nullable `*_ar` columns beside the existing base columns on `offers` (`title`,`body`), `redemption_items` (`name`,`description`), `teams` (`name`). Locale-aware accessor/trait: return the `_ar` value when `app()->getLocale()==='ar'` **and** it's non-empty, else fall back to the base column. Filament forms get paired EN/AR inputs (Tabs or grouped). Seed AR demo values so the walkthrough shows Arabic. Internal-only fields (candidate/parent names, notes, school, previous_club, season & point-rule names) stay single-language — out of scope. **Fold-in:** apply the Tajawal `@font-face` metric-override (fixes the button/label vertical mis-centering) in the admin branding render-hook — exact snippet + steps in the T12a review note below. |
| T12c | DONE | **API locale negotiation + localized payloads.** `SetApiLocale` middleware on `/api/v1` (read `Accept-Language`, `?lang` fallback, supported `[en,ar]`, mirror `SetPublicLocale`). PlayerResource (`playing_position`,`progress`) now localizes automatically via the translated enums; OfferController + RedemptionController return the locale-aware free text (via T12b accessor). Feature tests assert `Accept-Language: ar` → Arabic enum label + Arabic offer/reward text, default/en → English. |
| T13 | DONE | **Force candidate personal data to Latin/English (data consistency).** New `App\Rules\LatinText` (rejects Arabic-script chars; allows Latin letters/digits/spaces/`. - ' ,`) applied to `full_name`, `country_of_birth`, `citizenship`, `school`, `previous_club`, `parent_name` in BOTH the public registration `StorePublicRegistrationRequest` and the admin `CandidateForm`. Bilingual error message (EN + AR "please enter in Latin/English") so an AR-UI parent is told to type the name in English. No `_ar` columns — keeps one canonical field per value so the admin list / app / search stay uniform regardless of UI language. Decision (2026-07-02): personal data stays **single-language English**; only offers/rewards/team names are bilingual (T12b). |
| T12d | DONE | **Mobile: request locale + refetch on toggle.** Send `Accept-Language: <app locale>` on BOTH Dio clients (parent `dioProvider` + `staffDioProvider`) from `localeControllerProvider`. Invalidate/refetch server-data providers (offers, players, redemption items, points history) when the language toggle flips so already-loaded data re-localizes. Verify AR payloads render RTL (Directionality already handled). |
| T14 | DONE | **Registration form UX: derive year-of-birth + country/nationality selects** (PUBLIC registration form only; DB schema unchanged — `year_of_birth` column stays). **(1) Drop the redundant `year_of_birth` input:** remove it from `resources/views/public/register.blade.php`, from `StorePublicRegistrationRequest::rules()` + `attributes()`, and delete the `withValidator()` birth-year cross-check plus the now-dead `validation.birth_year_mismatch` key (EN **and** AR); **derive server-side** in `PublicRegistrationService::create()` as `Carbon::parse($data['date_of_birth'])->year` and keep persisting the existing `year_of_birth` column. `date_of_birth` stays `required\|date\|before_or_equal:today`. **(2) `country_of_birth` + `citizenship` become `<select>`s:** add `config/countries.php` (ordered `country ⇒ nationality/demonym` map of English canonical, LatinText-safe strings — must include `Qatar ⇒ Qatari`, the rest of the GCC, and a broad ISO-3166 set) + `App\Support\Countries::countries()` / `::nationalities()` (value=label maps, mirror `EnumOptions` style); controller `landing()`+`create()` pass `countryOptions`/`nationalityOptions` like `positionOptions`; blade replaces the two text inputs with selects (same `name`, `old()` selected, disabled empty placeholder, mirror the `playing_position` markup, drop the `js-latin-input`/`data-latin-message` attrs); validate `country_of_birth`/`citizenship` with `Rule::in(array_keys(...))` **replacing `LatinText` on only these two** — KEEP `LatinText` on `full_name`/`school`/`previous_club`/`parent_name` (T13 invariant, do not touch). **Tests** (`PublicRegistrationTest`): drop `year_of_birth` from `payload()`; assert a successful submit derives `year_of_birth === 2014` for `date_of_birth 2014-05-10`; add an invalid country/citizenship (not in list) → `assertSessionHasErrors` + no row; assert the rendered page has no `name="year_of_birth"` and has `<select name="country_of_birth">`. **Out of scope:** admin `CandidateForm` (optional T14b parity — leave as-is), migrations, and AR localization of country names (English labels in both locales; `dir="ltr"` on the selects, consistent with the Latin-letters convention). Branch `task/T14-registration-derive-year-country-selects` from current `main`; full `php artisan test` + `migrate:fresh --seed` green; EN/AR key parity held; commit before handoff; ≤10-line summary. |
| T14b | DONE | **Admin `CandidateForm` parity (depends on T15 — uses `App\Enums\Country`/`App\Enums\Nationality`; DB unchanged).** Mirror the two registration-form improvements to the Filament admin candidate form: **(1)** stop asking for `year_of_birth` — derive it from `date_of_birth` server-side (natural shared home = `CandidateDataNormalizer::normalize()`, which already runs on both the admin `Create/EditCandidate` mutate path **and** `PublicRegistrationService`; deriving there lets T14's service-level derivation become redundant/harmless) and drop/disable the `year_of_birth` `TextInput` in `CandidateForm` while still persisting the column; **(2)** replace the `country_of_birth` + `citizenship` `TextInput`s with searchable Filament `Select`s using `Country::options()` / `Nationality::options()` (the flag+label maps from T15) + `Rule::enum(Country::class)` / `Rule::enum(Nationality::class)` validation, `->required()`, and remove `LatinText` from **only** those two (keep it on `full_name`/`school`/`previous_club`/`parent_name`). **Make the selects tolerant of legacy stored values not in the enum** — include the record's current value in `->options()` so editing an old admin-entered candidate (e.g. a free-typed "USA") doesn't blank country/citizenship. Update `CandidateResourceTest` (`candidateFormData()` drops `year_of_birth`; assert derived year; country/citizenship use valid enum values); `CandidateInfolist` unchanged. Branch `task/T14b-admin-candidate-year-country` from `main` **after T15 merges**; full suite + `migrate:fresh --seed` green; ≤10-line summary. |
| T15 | DONE | **Country + Nationality enums (translatable + flag-emoji icons), shared by the public registration form + admin candidate form.** Replace T14's `config/countries.php` + `App\Support\Countries` with two **string-backed** enums whose **backing values equal the exact current stored strings — so existing data + seeders stay valid with NO migration:** `App\Enums\Country` (`case Qatar = 'Qatar'; case SaudiArabia = 'Saudi Arabia'; …` — GCC-first, then the same ~97-country list T14 shipped) and `App\Enums\Nationality` (`case Qatari = 'Qatari'; case Saudi = 'Saudi'; …`). Each implements `Filament\Support\Contracts\HasLabel`: `getLabel() => __('enums.countries.'.$this->name)` / `__('enums.nationalities.'.$this->name)` (**key by case name**, no spaces); add `flag(): string` returning the country flag **emoji** (Nationality maps to its country's flag) and a static `options(): array` = `[$case->value => trim($case->flag().' '.$case->getLabel())]` used by BOTH surfaces. Add matching `enums.countries.*` + `enums.nationalities.*` to `lang/{en,ar}/enums.php` (English + accurate MSA Arabic; **key parity both locales**). **Rewire the PUBLIC form only in this task:** controller passes `Country::options()`/`Nationality::options()`; `StorePublicRegistrationRequest` swaps the T14 `Rule::in(...)` for `Rule::enum(Country::class)` / `Rule::enum(Nationality::class)`; the blade keeps the `$countryOptions`/`$nationalityOptions` `<option>` loops but **drops `dir="ltr" lang="en"`** on these two `<select>`s so AR labels render RTL. **Icons = flag emoji in the label** (renders on mac/iOS/Android; degrades to the letter code on Windows — accepted, per decision). Delete `config/countries.php` + `App\Support\Countries` and update references. Keep the columns as strings — **no migration, no model casts, `CandidateInfolist`/admin display unchanged** (localized country display in the admin *view* is an optional later follow-up). Tests: `Qatar`/`Qatari` still validate via `Rule::enum`; invalid (`Atlantis`/`Atlantean`) still rejected; add an AR-render assert that a country option shows the Arabic label (e.g. `قطر`) + its flag; keep the no-`year_of_birth`-input / selects-render asserts. **Guard against duplicate demonyms** (Nationality case *values* must be unique — dedupe if the source list repeats one, e.g. multiple "…ian"s that collide). Branch `task/T15-country-nationality-enums` from current `main`; full `php artisan test` + `migrate:fresh --seed` green; `enums.php` EN/AR parity; commit before handoff; ≤10-line summary. |
| T16 | DONE* | **Restructure the Flutter app into a shared-core pub-workspace monorepo (2–3 demo apps).** `mobile/` becomes a Dart pub workspace: one shared package `mobile/packages/lfc_core` (~90% — auth/offers/players/redemptions/scan/session/staff, api/storage, providers, l10n, theme *engine*, shared fonts+flags) + thin shells `mobile/apps/demo_app_{one,two,three}` that depend on it. Only **branding** + **API base URL** vary per app, injected via `ProviderScope` overrides: `brandProvider` (no-default → `Brand{appTitle,palette,wordmark,logo,heroLogo}`) and `appConfigProvider` (→ `AppConfig.fromEnvironment(defaultBaseUrl:)`, keeps `--dart-define=API_BASE_URL` + T10 platform default); each app's `app.dart` boots `CoreApp` (was `LfcApp`). Distinct native IDs per app — `qa.lfc.app.demoone|demotwo|demothree` (Android appId+namespace+MainActivity path, iOS bundle id incl. `.RunnerTests`, display labels). **demo_app_one = today's app reassembled — behaviour + theme byte-identical to `main`.** Mobile-only; no backend. Briefs `briefs/T16.md` + `briefs/T16-fix.md`. **`DONE*` = code-complete & reviewed; on-device Android/dark walkthrough still pending (see review note).** |

### Demo App Two — Lusail SC supporter/fan app (epic; 2026-07-05)

**Context:** app one = academy recruitment + parent/player loyalty. **App two = the *same* club's fan-facing product** (self-registering supporters + admin-created VVIP members). Decisions locked with the user 2026-07-05: **(1)** shared Laravel app + DB, data **scoped by `app` column**, `/admin` split into per-app Filament panels; **(2)** attendance discount **reuses the append-only ledger + QR-scan engine** — each match scan appends **+0.5%** to the member's own account (balance = Σ, capped **10%**), shown as redeemable toward academy registration (**display only** — no in-app academy flow); **(3)** news/fixtures/table/results = **admin-managed CMS** read by app + guests; **(4)** VVIP = **showcase + digital card** (tiered, admin benefit catalog, non-transactional); **(5)** app two = **same club (Lusail SC)** — reuse app one's crest/wordmark/navy-gold *exactly* (overwrite the placeholder "Demo Two" brand). App one must keep working byte-identically.

| ID | Status | Task |
| --- | --- | --- |
| T17 | DONE | **App-scoping foundation (the spine — no behaviour change for app one).** Add an `App\Enums\AppKey` (`AppOne='app_one'`, `AppTwo='app_two'`) + an `app` string column (default `app_one`) to `parent_accounts`, `fixtures`, `offers`; **backfill all existing rows to `app_one`**. Add a reusable `BelongsToApp`/`ScopedToApp` trait (global scope keyed off a resolvable "current app" — panel/request context — plus an `app` attribute + `forApp()` scope) so app-one queries can never see app-two rows and vice-versa. Extend `App\Enums\AccountType` with `Member` + `VvipMember`; extend the ledger with a `unit` column on `point_transactions` (`points` default \| `discount_pct`) so a balance reads unambiguously. **No panel/API/mobile changes yet, no app-two data yet** — this task is migrations + enums + the scope trait + tests proving isolation and that the full existing suite (128) stays green on `migrate:fresh --seed`. Brief `briefs/T17.md`. |
| T18 | DONE | **Split `/admin` into per-app Filament panels + rename.** Re-home resources into `app/Filament/AppOne/**` (today's Candidates/Teams/Seasons/Fixtures/PointRules/ParentAccounts/Offers/Redemption*) and an empty `app/Filament/AppTwo/**`; each panel `discoverResources` only its own namespace (prevents auto-discovery leaking every resource into both panels). New `AdminAppOnePanelProvider` (id `admin-app-one`, path `admin-app-one`, `->default()`, behaviour identical to today) + `AdminAppTwoPanelProvider` (id `admin-app-two`, path `admin-app-two`, empty dashboard for now). Move the shared Lusail `brandStyles()` + colours into a base provider/trait both extend (one source of truth). Register both in `bootstrap/providers.php`; delete/redirect the old `AdminPanelProvider` (`/admin` → `/admin-app-one`). Fix every `/admin`-hardcode: `routes/web.php` locale-switch + candidate-doc download, `SetAdminLocale`, the `filament.admin.locale-switcher` view redirect target, and any `/admin`-referencing tests. Each panel resolves its own "current app" so T17's global scope filters correctly. Suite green; both panels load on dark+light; app-one panel is behaviourally unchanged. Brief `briefs/T18.md`. |
| T19 | DONE | **App-two members: open self-registration + attendance→discount accrual.** `POST /api/v1/auth/register` (open self-signup → `parent_accounts` `app=app_two`, `account_type=member`; app one stays invite-only). A fixed **+0.5%** `PointRule` scoped to `app_two`; branch `ScanController::scan()` by account type — app-one parent → credit linked players (**unchanged**); app-two member → append `+0.5%` to the member's own account via `PointsEngine` (`unit=discount_pct`), reusing `attendance_scans` dedupe (one scan per member per fixture) with a **10% cap** guard. `/me` returns the member's accrued discount %. App-two admin: **Members** resource (accounts where `app=app_two`) + read-only **discount ledger** relation. Tests: register creates a scoped member; scan accrues 0.5% once per fixture; cap enforced; app-one scan path untouched. Depends on T17+T18. Brief `briefs/T19.md`. |
| T20 | DONE | **App-two content CMS + guest API.** New `app`-scoped tables `news_posts` (title/body AR+EN, hero image, published_at) + `standings` (team/played/won/drawn/lost/gf/ga/points/position); extend `fixtures` for app-two public matches (opponent, competition, home/away, our_score, opp_score, kickoff). Filament resources in the app-two panel (bilingual, mirroring the T12b paired EN/AR pattern). **Guest-accessible** (no auth) API: `GET /content/news[/{id}]`, `/content/fixtures`, `/content/results`, `/content/standings`, app selected via an `X-App-Key: app_two` header. Tests: guest can read content without a token; content is app-two-scoped. Depends on T17+T18. Brief `briefs/T20.md`. |
| T21 | DONE | **App-two VVIP membership: tiers + benefit catalog + digital card.** New `app`-scoped `membership_tiers` (name AR+EN, level, e.g. Gold/Platinum) + `membership_benefits` (tier, title, description AR+EN, icon); `parent_accounts` gains `membership_tier_id` + digital-card fields (member number, valid-until). Admin creates `vvip_member` accounts (no self-registration) + manages the benefit catalog. `GET /me/benefits` returns tier + benefits + card payload; app-two Offers reuse `Offer::visibleTo()` (VVIP-only stays hidden from normal members). Benefits are **display-only** (non-transactional). Tests: benefit payload scoped to the member's tier; VVIP-only offers hidden from `member`. Depends on T17+T18. Brief `briefs/T21.md`. |
| T22 | DONE | **App-two mobile shell + Lusail branding + guest Home/Matches.** Overwrite `mobile/apps/demo_app_two/lib/brand.dart` to the **exact Lusail SC brand** (crest/wordmark/navy-gold, same as demo_app_one). App two gets its **own guest-first shell** (app one's `CoreApp` is app-one-specific) with tabs: **Home** (club news/latest) + **Matches** (fixtures / results / league table), both reading the T20 guest API with **no login required**. Shared theme/api/l10n/session come from `lfc_core`. AR+EN/RTL inherited. `flutter analyze` clean; smoke + widget test (guest home renders news). Depends on T20. Brief `briefs/T22.md`. |
| T23 | TODO | **App-two mobile member auth + discount wallet.** Register + login screens (self-signup via T19). Login-gated **Membership/Wallet** tab for normal members: accrued academy-registration **discount % card**, rotating **QR** (reuse `ScanTokenService` widget) shown at matches, discount/points history. Reuse `lfc_core` auth/session/QR primitives. Widget test: member card shows the accrued %. Depends on T19+T22. Brief `briefs/T23.md`. |
| T24 | TODO | **App-two mobile VVIP card + benefits.** For `vvip_member` accounts: **digital membership card** (tier, member #, valid-until, QR) + benefits list (from T21 `/me/benefits`) + VVIP offers. Widget test: VVIP card shows tier + benefits. Depends on T21+T22. Brief `briefs/T24.md`. |
| T25 | TODO | **App-two seed + walkthrough addendum.** Seed app-two demo data (AR+EN news, past results + upcoming fixtures, a standings table, a demo `member` with some accrued discount, a demo `vvip_member` with tier/benefits/card), plus demo logins; add an app-two section to `docs/DEMO-WALKTHROUGH.md` (guest browse → register → scan → discount → VVIP card) guarded by an invariants test. Depends on T19–T24. Brief `briefs/T25.md`. |

## Dependencies

T1 → T2 → T3 → (T4–T8 partly parallel) → T9a → T9b → T10. **T7b** (VVIP client accounts) depends on T5 (ledger) + T7 (VVIP/offers + redemption) and should land **before T8** so the dashboard counts account-level points/liability.

**Registration/candidate follow-ups (2026-07-02):** T14 (DONE) → **T15** (country/nationality enums, translatable + flag icons; rewires the public form) → **T14b** (admin candidate form consumes the T15 enums + derives year-of-birth). T15 replaces T14's `config/countries.php`/`App\Support\Countries`; both later tasks are one-form-each so they don't collide.

**Bilingual (post-demo requirement, 2026-07-02):** T12a → T12b → T12c → T12d, strictly sequential. Enum-label translation (T12a) and dual-language free-text storage (T12b) both feed the localized API payloads (T12c); the mobile client (T12d) consumes T12c. Scope decision = **app-facing content only** (enums + the free text a parent actually sees: offers, rewards, team names); internal-only admin free text stays single-language.

**Demo App Two epic (2026-07-05):** **T17 (app-scoping) → T18 (panel split)** are the spine — both must land before any app-two feature. Backend features **T19 (members+discount) / T20 (content CMS) / T21 (VVIP)** each depend on T17+T18 and are independent of each other (parallelizable). Mobile: **T22 (shell+guest content)** depends on T20; **T23 (member wallet)** on T19+T22; **T24 (VVIP card)** on T21+T22; **T25 (seed+walkthrough)** last, on T19–T24. Land T17 first (everything scopes against it). App one stays byte-identical throughout — the panel split (T18) and scan branch (T19) must not regress the existing 128-test suite.

## PM review checklist

- Matches §7/§8 scope — no over-build beyond the demo cut.
- Ledger integrity: balance = Σ transactions; no direct balance edits. **Each ledger row has exactly one owner (player XOR account); per-player path unchanged.**
- Scan: one-scan dedupe; correct player credited for the match's team.
- VVIP-only offers hidden from non-VVIP accounts. **VVIP client (non-parent) accounts: see VVIP offers + can redeem from an account balance; admin grant is gated + audited.**
- Private document access enforced.
- AR/RTL renders correctly.
- **Bilingual (T12):** admin UI + all enum labels translated EN/AR and RTL flips for `ar`; app-facing free text (offers/rewards/team names) stored per-language with base-column fallback and served by request locale; internal-only fields untouched; no regressions to the append-only/XOR-owner/VVIP-visibility invariants.

## Carry-over follow-ups

- ~~T1 → T2: widen `User::canAccessPanel()` beyond Admin~~ — **DONE in T2** (`hasAnyRole(['Admin','Coach','Management'])`).
- ~~T1 → T2: documents on the `private` disk~~ — **DONE in T2** (private disk + authenticated streamed download).
- ~~T2 fold-ins: doc-type "Parent QID/passport", hide recruitment_stage on create, download test~~ — **DONE in T3** (9 doc types; signed+auth download with exact-bytes test).
- **T4 (2026-06-30):** recovered from a branch mishap that had frozen `main` at an old commit; `main` is now correct (T1–T4). Trivial nit left in `FixtureSeeder` (`$season` looked up but unused — uses `$team->season_id`).
- **From T3 (review fix):** `LFC_DEMO_PARENT_EMAIL/PASSWORD` were in README but not `.env.example` — **added to `.env.example` and the local `.env`**. Seeder falls back to defaults `parent.demo@lfc.test` / `password`.
- **Stack note:** running on Laravel 13 / PHP ^8.3 (latest stable — fine).
- **T5 (2026-06-30) review:** APPROVED & merged (50 tests green; `migrate:fresh --seed` clean). Ledger append-only (model `updating`/`deleting` throw), balance = Σ txns (no stored column), rules fixed/percentage scoped+dated with deterministic resolution, percentage off admin-entered `base_amount`, Adjust-points action gated Admin/Management + audited.
  - **Defect caught:** committed branch tip imported non-existent `Filament\Tables\Actions\{Edit,Delete}Action` (would fatal the Point Rules page); a fix was sitting **uncommitted** in the working tree — committed it as `73969b1` during review. *Process feedback for opencode: commit ALL working-tree changes before handing off; the broken tip passed tests because no test renders the Filament table.*
  - **Open follow-ups (non-blocking):** Adjust action authorized via `->visible(hasRole)` only (consider `->authorize()`/policy); append-only guard is Eloquent-event level (raw query-builder `update()`/`delete()` would bypass — DB-level hardening deferred); `scopeActiveOn()` only filters `is_active` (date window lives in `resolveRule`) — naming nit. Manually click-through the Point Rules page during T10/verify to confirm render.
- **T6 (2026-06-30) review:** APPROVED & merged (73 tests green; `migrate:fresh --seed` clean). `attendance_scans` with `unique(parent_account_id, fixture_id)` dedupe; `ScanTokenService` HMAC-SHA256 token (`hash_equals` + TTL/skew freshness); `POST /staff/login` (role-gated), `GET /scan-token` (parent-only), `POST /scan` (staff-only, role-gated) — both token-type directions enforced via `instanceof`; credits only linked players on the fixture's team via `PointsEngine::credit()` (scan = morph source); attendance still recorded with 0 pts when no rule.
  - **Defect process (repeat of T5):** entire T6 arrived **uncommitted** (3 modified + 7 untracked) on a branch tip still at the T5 merge — preserved as WIP checkpoint `5b03e40` before reviewing. Coder also mis-reported "4 pre-existing test failures" — actually 0 on a clean `RefreshDatabase` run (dirty local test DB). *Reminder to opencode: commit before handoff; run tests on a fresh DB.*
  - **Open follow-ups for opencode (fold into start of T7; non-blocking for demo):** (1) **wrap scan-create + credit loop in `DB::transaction()`** — currently the scan commits before crediting, so a mid-loop `credit()` failure leaves the parent permanently under-credited + dedupe-locked (brief asked for this); (2) dedupe race-catch matches a MySQL-specific index name in the exception message → use driver-agnostic `catch (Illuminate\Database\UniqueConstraintViolationException $e)`; (3) qualifying-players query should defensively `->where('is_player', true)` and qualify `candidates.team_id`. — **ALL 3 DONE in T7 commit `7e646e4`.**
- **T7 (2026-06-30) review:** APPROVED & merged (98 tests green; `migrate:fresh --seed` clean; handoff properly committed in 5 commits this time). Redemption catalog (`redemption_items` fee/event/merch + `redemptions` w/ unique voucher) — `RedemptionService::redeem()` in `DB::transaction`+`lockForUpdate`, balance/stock/validity guards, typed exceptions → 403/422; redeem deducts via new `PointsEngine::redeem()` (`-abs(points)`, append-only, scan/redemption morph source). VVIP = Admin-gated `is_vvip` flag on `parent_accounts`, exposed in `/me`; `offers` + `Offer::visibleTo()` filters **server-side** so non-VVIP never receive `vvip` (tested through the real `/api/v1/offers` endpoint). T6 fixes (1)(2)(3) folded in.
  - **Open follow-ups (non-blocking for demo):** (a) residual same-player double-spend window — balance check is in the txn but the lock is on the *item* row; concurrent redemptions of *different* items for the *same* player don't serialize on balance → add `Candidate::lockForUpdate()->find($player->id)` at the top of the txn; (b) `RedemptionController::items()` lacks the `instanceof ParentAccount` guard the other parent endpoints have (harmless — public catalog). — **(b) DONE in T7b; (a) partially addressed in T7b — see below.**
- **T7b (2026-06-30) review:** APPROVED & merged (113 tests green = 98 existing untouched + 15 new; `migrate:fresh --seed` clean; properly committed in 5 commits). Non-parent VVIP clients: `account_type` (parent|vvip_client) on `parent_accounts`; ledger owner = **player XOR account** (`point_transactions.parent_account_id` added, `candidate_id` made nullable via robust drop-FK→change→re-add; exactly-one-owner `creating` guard); `PointsEngine::grantToAccount()`/`redeemFromAccount()`; `RedemptionService::redeemForAccount()` (txn + `lockForUpdate` item+account); `POST /redemptions` `player_id` optional (vvip_client → account path, plain parent w/o player_id → 422); Filament account_type select auto-sets `is_vvip`, Admin-gated GrantPoints action; `/me` exposes account_type + account balance; demo seed `vvip.demo@lfc.test` (500 pts). **Per-player path untouched (regression test + no existing test files modified).**
  - **Open follow-ups (non-blocking for demo):** (a) the T7 player-path lock was added on the **parent** row, not the **player** row — since `parent_player_links` is many-to-many, two parents redeeming the same shared player's points concurrently won't serialize; full fix = lock the `candidates` row in `RedemptionService::redeem()` (the account path IS correctly locked). (b) no model-level guarantee that `account_type=vvip_client ⇒ is_vvip=true` — Filament form + seeder set it, but a `saving` hook would enforce it for any other creation path.
- **T8 (2026-06-30) review:** APPROVED & merged (121 tests green = 113 + 8 new; widgets render-verified via class resolution + `getStats()` build, since no test renders them — the T5 lesson). `LoyaltyMetrics` service (6 pure-DB aggregates) + `LoyaltyStatsOverview` (StatsOverviewWidget) + `PendingFulfillmentsTable` (TableWidget, read-only). **All point totals aggregate the full `point_transactions` table (both owners) — verified on seeded data (issued/liability include the 500 account-owned VVIP grant) and via `outstanding_liability_equals_full_ledger_sum` test.** Read-only; no migrations/API.
  - **Open follow-ups (non-blocking):** (a) `vvipClients()` counts `is_vvip=true` (all VVIP accounts) rather than the brief's `account_type='vvip_client' OR is_vvip=true` — fine in practice, label slightly broad; (b) no `canView()` gating on the stats widget — liability is visible to Coaches, not just Admin/Management (optional hardening).
- **T9a (2026-07-01) review:** APPROVED & merged (`45e9998`). **First Flutter task — I ran the real toolchain this time:** `flutter analyze` = 0 issues, `flutter test` = 7/7 green (4 repository + 3 widget/RTL), re-confirmed on `main` post-merge. `mobile/` app (iOS+Android): Riverpod + Dio + flutter_secure_storage + flutter_localizations/intl. Dio Bearer interceptor (secure-storage token; **401→clear token + global `emitUnauthorized`→session logout**; **422→field errors** surfaced under form fields); session routing (splash/login/home via `SessionStatus`). Login + accept-invite screens (client-side `min:8` matches backend), `/me` home shell (name/account_type/VVIP/balance). **AR+EN with in-app RTL toggle — RTL directly asserted in a widget test (`Directionality.of(...)==rtl` after switching to Arabic).** Correctly handles the two API shapes: login's `parent` is a *nested* resource (flat, no `data` wrap) vs `/me` top-level resource (`data`-wrapped, `unwrapData`). **Cleartext HTTP handled on both platforms** (Android `usesCleartextTraffic=true`+INTERNET; iOS `NSAllowsLocalNetworking`) so the demo actually connects to `php artisan serve` over http — the usual first-run blocker, pre-solved. Per-platform base URL documented in README (`--dart-define`, iOS localhost / Android 10.0.2.2). Handoff properly committed (2 commits: scaffold + email-validation fix). No feature screens (correct scope).
  - **Cleanup done during review:** the coder's branch carried **uncommitted out-of-scope `flutter create` desktop/web scaffolding** (`linux/macos/web/windows/` + a `.metadata` platform-additions edit) beyond the iOS+Android brief — NOT merged; stashed as `stash@{0}` ("T9a out-of-scope…") so it's recoverable. Can be `git stash drop`ped. *Process note for opencode: `flutter create` defaults to all platforms — pass `--platforms=ios,android`, and don't hand off uncommitted generated dirs.*
  - **Open follow-ups (non-blocking; candidates for T9b):** (a) **locale not persisted** across restarts (resets to EN) — persist via secure-storage/prefs; (b) `dioProvider` interceptor's `onResponse`-reject + `emitUnauthorized` path isn't directly unit-tested (repo tests use a bare Dio and exercise the `fromObject` fallback) — repo is robust either way, but an interceptor test would close the gap; (c) `SessionController.build()` starts a bootstrap microtask + stream subscription (side-effects in `build`) — works because it only watches stable providers, slightly non-idiomatic Riverpod. (d) **Runtime device smoke-test still owed by the user** — I verified analyze/test/logic statically + via the test suite, but did not run a simulator/emulator; confirm login→/me→AR-RTL toggle→logout on a device against a running API before the demo. **[(a) locale persistence DONE in T9b.]**
- **T9b (2026-07-01) review:** APPROVED & merged (`0e40191`). Ran the real toolchain: **`php artisan test` 127 green** (121→127; +6 backend), **`flutter analyze` clean, `flutter test` 14 green** (7→14; +7 feature tests), re-verified on `main`. **Backend (minimal, task-scoped):** `points_balance` on `PlayerResource`; new `PointTransactionController` with **parent-scoped** ledger endpoints `GET /players/{player}/transactions` (re-scoped via `$parent->players()->whereKey()->firstOrFail()` → **404 for non-linked player**, tested) + `GET /me/transactions` (account-owned rows only); `GET /staff/fixtures` (staff-gated like `scan`). Tests cover balance=ledger-sum, per-player isolation, account/player XOR separation, and the non-linked-player 404. **Mobile:** T9a home stub → bottom-nav parent/VVIP shell (Players+balances, rotating QR w/ 1s tick + expiry/resume/pull refetch, points history, rewards redeem→voucher, offers) + **isolated staff mode** (login, fixture pick, manual-paste **and** camera scan, credited results). **Token isolation done right:** separate `dioProvider` (parent, `auth_token`) vs `staffDioProvider` (staff, own storage) with separate 401 event channels — no clobber. Redeem correctly branches on `account_type` (parent sends `player_id` [required], vvip_client omits it → account path). Locale persisted across restarts. `qr_flutter` + `mobile_scanner` added with Android CAMERA + iOS `NSCameraUsageDescription`. **AR/EN: 104/104 keys both files, none untranslated.** Handoff clean (1 commit, worktree clean, no stray desktop/web dirs).
  - **BUG (non-blocking, one-line fix, fix before demo):** `ApiException` has **no `toString()` override**, but **6 screens render load errors via `error.toString()`** (offers/rewards/players/points-history/staff-scanner/qr) → any failed fetch shows the user literally **"Instance of 'ApiException'"**. Only hits error paths (happy path + redeem-422 use `.message`, so tests pass) — but a misconfigured API base URL (esp. Android `10.0.2.2`) makes *every* list show it, i.e. it degrades exactly the most likely live-demo failure. **Fix = add `@override String toString() => message;` to `ApiException`** — repairs all 6 call sites at once. *Recommend opencode apply this one-liner at the top of T10.*
  - **Open follow-ups (non-blocking):** (a) `PlayerResource.points_balance` runs a `SUM` query per player in the list (N+1) — negligible for demo's few players; (b) staff login routes through the parent Dio (`authRepositoryProvider`) — harmless since `/staff/login` ignores bearer tokens, minor inconsistency; (c) **device camera/QR scan smoke-test still owed** — I can't exercise the camera or on-device QR headless; run the full parent+staff walkthrough on a device before the demo.
- **T10 (2026-07-01) review:** APPROVED & merged (`0a3f807`) — **final task; the T1–T10 demo build is complete.** Ran the real toolchain: **`php artisan test` 128 green** (+1: `DemoSeederInvariantsTest`), **`flutter analyze` clean, `flutter test` 16 green** (+2: `ApiException.toString`, Fix-C players-refresh), re-verified on `main`. **Fix A** platform-aware base URL (`app_config.dart`: honors `--dart-define=API_BASE_URL`, else Android→`10.0.2.2` / else `localhost`) — the plain Run button now works on Android. **Fix B** `ApiException.toString()=>message` (repairs the 6 `error.toString()` screens). **Fix C** stale balances: `players_screen` migrated to a shared `playersProvider = FutureProvider.autoDispose` (`ref.watch`+`.when`); `SessionController.refreshAccount()` re-fetches `/me`; redeem success + staff-scan success call `ref.invalidate(playersProvider)` (+ `refreshAccount()` on redeem) — validated by an **end-to-end widget test** (150→redeem→140 across the IndexedStack). **Seed invariants** guaranteed + guarded by `DemoSeederInvariantsTest`: demo player explicitly placed on the open fixture's team; scan window widened (`now-1d`..`now+7d`); balances **150** (Omar: +200 −50 backpack) / **500** (VVIP: +520 −20 bottle); valid all+VVIP offers; one **issued** (Omar) + one **fulfilled** (VVIP) redemption for the dashboard. **`docs/DEMO-WALKTHROUGH.md`** added (setup w/ `--host=0.0.0.0`, credentials, 4 acts, exact numbers incl. the VVIP header 500→300 that demonstrates Fix C). Handoff clean (1 commit, cut from main, no stray files). Note: `dart format` reflowed many mobile files (formatter-only noise; analyze clean).
  - **Still owed by the user (only thing left):** the on-device run of the full walkthrough — camera/QR scan (parent shows QR, staff scans) on a real emulator/device. Everything else is verified.
- **T11 (2026-07-01) review:** APPROVED & merged (`d23e8ef`) — 3 post-UAT mobile-only fixes found in device testing. `flutter analyze` clean, `flutter test` 16 green, `php artisan test` 128 unchanged (verified on `main`). (1) `home_shell` header: `HomeShell` watches `playersProvider`; parent → **"Total points"** = Σ players' `pointsBalance` (auto-refreshes via Fix C invalidation, `'...'` while loading), `vvip_client` → **"Account balance"** (`account.accountBalance`) — fixes the always-`0` chip parents saw. (2) `rewards_screen._friendlyError`: added a `422 && message.contains('insufficient')` branch → new `redeemInsufficientError` (EN+AR, ARB parity 105/105) before the generic "unavailable" catch-all. (3) VVIP chip wrapped in `if (account.isVvip)` (no more "VVIP: No" noise). `account!` in the VVIP points branch is null-safe (only reached when `isVvipClient`).
  - **Process (repeat):** handed off **uncommitted on `main`** (no `task/T11` branch) — I preserved it on `task/T11-uat-fixes` (`311c00b`) before review, then merged. *Nth reminder to opencode: work on `task/T<id>` and commit before handoff.*
  - **Open follow-up (non-blocking):** brief asked for 3 fix-specific tests; none added — existing header assertions were only weakened `findsOneWidget`→`findsAtLeastNWidgets(1)` (because the players-total now also renders in the header). Fixes are correct + green. **~~Fix-2 error-mapping test~~ DONE** (2026-07-01, `737d384` — PM-authored one-off at the user's request; asserts insufficient→`redeemInsufficientError` and NOT `redeemUnavailableError`; suite now 17 Flutter tests). A Fix-1/Fix-3 header test remains an optional nicety.
- **T12a (2026-07-02) review — APPROVED & merged (`c2a3a0f`, ff to main; 139 tests green).** Branch `task/T12a-admin-bilingual-foundation` (`939a4ab` + fix `c2a3a0f`). **Strong overall:** `SetAdminLocale` mirrors `SetPublicLocale` and sits right after `StartSession`; TOPBAR_END EN/AR switcher + `/admin/locale/{locale}` route; all 13 enum `getLabel()`s → `__('enums.*')` with `getColor()` left intact; `Candidate::publicProgressLabel()` translated; `lang/{en,ar}/admin.php` **208/208 key parity** (only `common.not_available` identical, expected) + complete `enums.php` with accurate MSA matching the locked glossary; **139 tests green** incl. enum EN/AR asserts + a `/admin/login?lang=ar` RTL assert. **PM ran the manual browser check the coder skipped** (temp local auto-login route, reverted; headless AR shots): dashboard, candidates **table**, and candidate **form** all render Arabic + RTL correctly (switcher flips to top-left, right-aligned, `مقبول` badge, `المرشحون`/`إضافة مرشح`/`الاسم الكامل`, etc.).
  - **DEFECT — FIXED in `c2a3a0f`** (cast added + `playingPositionLabel()` simplified to `$this->playing_position?->getLabel()`; PM re-verified in-browser: the AR candidates table badge now shows `لاعب وسط`/`مهاجم`/`مدافع`; 139 tests green). Original: `Candidate.playing_position` was the **only** enum attribute not cast to its enum (`casts()` omits it), so the candidates-table `->badge()` (and the infolist entry) print the raw English value (`midfielder`/`attacker`/`defender`) inside an otherwise-Arabic table, while the form dropdown *is* Arabic (options via `EnumOptions`) — a visible EN/AR inconsistency. **Fix (2 lines):** add `'playing_position' => PlayingPosition::class` to `Candidate::casts()`, AND change `playingPositionLabel()` to `return $this->playing_position?->getLabel() ?? '';` (once cast, `getAttribute('playing_position')` returns the enum, so the old `PlayingPosition::from(...)` would TypeError). Re-run `php artisan test`. (All other enum attrs — offer audience, redemption/rule/txn type, fixture/redemption status, account_type — are cast, so they localize fine.)
  - **Minor (non-blocking):** the switcher is TOPBAR_END, so it's absent on the **login** page (a fresh admin can't click to Arabic pre-login) — `?lang=ar` still flips it, so acceptable for the demo.
  - **Fold-in for T12b — Tajawal button/label centering fix** (the `->font('Tajawal')` mis-centering I investigated 2026-07-02; see [[tajawal-metric-ride-high]]). Self-host Tajawal (copy `mobile/assets/fonts/Tajawal-{Regular,Medium,Bold}.ttf` → `public/fonts/tajawal/`) and add this to the branding render-hook `<style>` (HEAD_END) so it shadows Filament's Bunny copy per (family,weight):
    ```css
    @font-face{font-family:'Tajawal';font-weight:400;src:url('/fonts/tajawal/Tajawal-Regular.ttf') format('truetype');ascent-override:80%;descent-override:22%;line-gap-override:0%;}
    @font-face{font-family:'Tajawal';font-weight:500;src:url('/fonts/tajawal/Tajawal-Medium.ttf') format('truetype');ascent-override:80%;descent-override:22%;line-gap-override:0%;}
    @font-face{font-family:'Tajawal';font-weight:600;src:url('/fonts/tajawal/Tajawal-Bold.ttf') format('truetype');ascent-override:80%;descent-override:22%;line-gap-override:0%;}
    @font-face{font-family:'Tajawal';font-weight:700;src:url('/fonts/tajawal/Tajawal-Bold.ttf') format('truetype');ascent-override:80%;descent-override:22%;line-gap-override:0%;}
    ```
    Keep `->font('Tajawal')` (it still sets `--font-family` + preconnect); the override only corrects the metrics. `line-gap-override:0%` does most of the work (Tajawal ships a 0.2em gap); values aren't sensitive (78–83% / 20–24% all recenter). Verified by pixel-measuring the login "Sign in" button: raw Tajawal rode ~2.5px high; override recenters it to match a normal font.
- **T12b (2026-07-02) review — APPROVED & merged (`2d275dc`, ff to main; 142 tests green, `migrate:fresh --seed` clean).** Branch `task/T12b-bilingual-content-storage`, cut from `0cab04d`. Nullable `*_ar` beside base cols on `offers`(title,body)/`redemption_items`(name,description)/`teams`(name), no renames; `HasLocalizedContent::localized($field)` trait (AR when locale=ar & filled, else base) wired into the 3 models + `*_ar` added to each `#[Fillable]`; paired **optional** EN/AR form inputs; list/infolist display via `->state(fn ($r) => $r->localized(...))` keeping search/sort on the base col; `admin.php` +5 keys each side (**213/213 parity**); seeders write AR demo content (3 offers / 5 rewards / 2 teams, `updateOrCreate` keys unchanged); `LocalizedContentTest` covers ar/fallback/en; **Tajawal font fold-in self-hosted under `public/fonts/tajawal/`**. API controllers/resources + mobile untouched (correct — that's T12c/T12d).
  - **PM browser-verified (AR):** offers list shows the seeded Arabic titles (`خصم التسجيل المبكر`, …) + Arabic audience badges, RTL; create form shows the paired `العنوان` / `العنوان (بالعربية)` inputs (AR optional). **Font fold-in confirmed working live** — the `@font-face` override wins the cascade and recenters the "Sign in" button to gap 26/23 (matches a normal font). *(Measurement note for future me: verify the override by serving the page with the font **same-origin**; injecting a `<base href>` into a saved copy breaks the local `@font-face` src resolution and makes Chrome fall back to Bunny's Tajawal → a false "still rides high" reading.)*
  - **Reminder for T12c:** the API mappers (`OfferController::index`, `RedemptionController::items`) still return `$offer->title` / `$item->name` directly, and `PlayerResource` returns `team?->name` — swap these to `->localized(...)` and add `SetApiLocale` so the app receives AR when it asks.
- **T12c (2026-07-02) review — APPROVED & merged (`3b663b4`, ff to main; 147 tests green).** Branch `task/T12c-api-locale-negotiation`, cut from `f8d90c3`. Stateless `SetApiLocale` (`?lang` → `getPreferredLanguage(['en','ar'])` → fallback `en`; no session), aliased `api.locale` in `bootstrap/app.php` + added to the `/api/v1` group (`['throttle:api','api.locale']`). Free-text swapped to `->localized(...)` in exactly the 6 approved spots: `OfferController`(title,body), `RedemptionController`(items name/desc, redeem item.name, history item.name), `PlayerResource`(team_name), `ScanController`(staff-fixtures team_name). `playing_position`/`progress` auto-localize via the T12a enums (no code change). Enum `.value` keys (audience/type/status/account_type), person names, and `PointTransaction.reason` correctly left untouched. `ApiLocaleNegotiationTest` (5 tests): offers/rewards/players/staff-fixtures ar+en, no-header→en default, `?lang` overrides header, `ar-QA` negotiates to `ar`.
  - **PM live-verified:** `curl /api/v1/offers` with `Accept-Language: ar` → Arabic titles (`خصم التسجيل المبكر`…), `en` → English, **no header → English** (backward-compatible; mobile keeps working until T12d). VVIP visibility still correct (non-VVIP demo parent saw only the 2 all-audience offers).
  - **Only T12d left:** mobile sends `Accept-Language` from the app locale on both Dio clients + refetch content providers (offers/players/rewards/history) on locale toggle so already-loaded lists re-localize. Note for T12d: the app also shows enum `.value` badges (redemption `type`/`status`, offer `audience`) which the API sends as English keys — decide whether the app maps those client-side to AR (it likely already localizes its own chrome; these fixed values may need ARB entries).
- **T12d (2026-07-02) review — APPROVED & merged (`686de74`, ff to main).** Branch `task/T12d-mobile-locale-header`, cut from `7b006a9`; **mobile-only** (no `app/`/`routes/`/`database/`/`lang/` touched). `Accept-Language: ref.read(localeControllerProvider).languageCode` added to **both** `dioProvider` + `staffDioProvider` `onRequest` (per-request `read`, no Dio rebuild). Refetch-on-toggle: `home_shell` `ref.listen` → `ref.invalidate(playersProvider)`; `offers_screen` + `rewards_screen` re-assign their `FutureBuilder` future via `ref.listen` + `setState`. Voucher `status` (issued/fulfilled/cancelled) now mapped through a `_voucherStatusLabel` helper + new ARB keys (صادرة/مستوفاة/ملغاة). *(`_transactionTypeLabel` already covered all 5 types + ARB already had expire/adjust/reverse — my briefing grep was truncated; no change needed, correctly left alone.)*
  - **PM-verified here (Flutter 3.44.4):** `flutter analyze` clean; **`flutter test` 20 green** (17→20: +2 `dio_locale_test` asserting both clients send the current `Accept-Language` and flip en→ar on `setLocale`, +1 "offers tab refetches content after locale toggle" widget test; RTL toggle tests still green). ARB **112/112 parity**.
  - **Only remaining item = the user's on-device check** (as with every mobile task — I can't drive the emulator/API headless): log in against a running API, toggle to AR, confirm offers/rewards/players show Arabic (and revert on EN).
- **✅ T12 bilingual arc COMPLETE (T12a–d all merged).** Admin panel is fully EN/AR + RTL with translated enum labels; app-facing free text (offers/rewards/team names) is stored per-language and served by request locale; the mobile app requests its locale and re-localizes on toggle. Backend 147 PHPUnit green; mobile `flutter analyze` clean + 20 tests green. Pending only the on-device AR walkthrough.
- **T13 (2026-07-02) review — APPROVED & merged (`c384945`, ff to main; 157 tests green).** Branch `task/T13-latin-personal-data`, cut from `6a19a2e`. `App\Rules\LatinText` (implements `ValidationRule`; rejects Arabic-script Unicode ranges `0600–06FF/0750–077F/08A0–08FF/FB50–FDFF/FE70–FEFF` via `preg_match(/u)`; empty passes; message = `__('validation.latin_only')`) applied to the 6 personal-data fields (`full_name`, `country_of_birth`, `citizenship`, `school`, `previous_club`, `parent_name`) in BOTH `StorePublicRegistrationRequest` and admin `CandidateForm` (`->rule(new LatinText)`). New `lang/{en,ar}/validation.php` + `validation.latin_only` keys in public-registration.php & admin.php (parity held: admin 214/214). Tests: `LatinTextTest` (9 cases incl. Arabic-Indic digits fail) + feature `test_arabic_full_name_is_rejected_with_validation_error` (422 + no row). No migration/schema/mobile.
  - **PM caught + cleared the one real risk:** a partial `lang/en/validation.php` (only `latin_only`) *could* have silently blanked Laravel's built-in messages (`required`/`email`/…) app-wide — tests wouldn't catch it (they assert error *presence*, not text). Verified empirically: Laravel 12 still merges framework defaults, so all built-ins resolve correctly alongside `latin_only`. No regression.
  - **Decision recorded:** candidate/player **personal data stays single-language English** (names/school/club); only offers/rewards/team names are bilingual (T12b). This rule enforces that so the admin list / app / search stay uniform whatever the UI language. A parent using the AR form gets an Arabic message telling them to type the name in Latin.
- **Registration page redesign (2026-07-02, `acb501e`)** — not a backlog task; user-requested. Replaced the generic academy hero copy with Lusail SC's actual **"Player Recruitment"** letter (from the club's Google recruitment form), fully bilingual (EN + AR MSA) via restructured `public-registration.hero.*` keys, and added the form's dark sporty banner (`public/images/registration-hero.png`) as a mask-faded full-bleed header on the navy hero card. Verified EN/AR (RTL) + mobile render. Built in an **isolated git worktree off main** (branch `task/registration-hero`) to avoid colliding with the live T13 session, then cherry-picked onto main (clean auto-merge of `public-registration.php` — different section from T13's `validation` key). **Note:** `public/build` is gitignored → run `npm run build` at deploy to compile the hero CSS.
- **Admin sidebar (2026-07-02, `5c25abd`)** — salvaged an uncommitted `->sidebarCollapsibleOnDesktop()` working-tree change (not part of T13) into its own commit so it wasn't lost/misattributed. *Recurring reminder to opencode: commit ALL working-tree changes before handoff.*
- **Season registration token bug (2026-07-02, `100aadb`)** — **demo-critical**, found during the registration-page work. `DatabaseSeeder` uses `WithoutModelEvents`, which muted the `Season::creating` hook that generates `registration_slug` → the seeded active season had a **null token** → `publicRegistrationUrl()` null → the public registration page 500'd (`UrlGenerationException: missing registrationSlug`); admin Season edit showed an empty "Registration token". Fix (4 parts + 3 regression tests, 160 green): (1) model generates the token on **`saving`** (not just `creating`) with a `blank()` guard so a token-less season self-heals on the next admin save; (2) `SeasonSeeder` sets it explicitly (hook is muted during seeding), guarded so re-seeds keep a stable link; (3) **backfill migration** for existing null-token seasons (fixes live DBs via `php artisan migrate`, no re-seed); (4) public page guards the form on a present token and shows a clear **bilingual** "registration not available yet" message instead of 500'ing. All merged to main; the full **T1–T13 build + bilingual arc + registration redesign are complete on `main`**.
- **Registration UX polish (2026-07-02)** — user-requested, not a backlog task. Public form now: client-side Latin validation mirroring `LatinText` (live per-field + submit guard, in `resources/js/app.js`); field-aware error messages (`LatinText` is now `ValidatorAwareRule`, injects `:attribute`) + per-field red highlight + top error summary; on validation error the page scrolls to the form card and focuses the first invalid field; one "Enter names & places in English (Latin) letters" note beside each section title (`.lfc-section-note`); **all fields now required** (incl. `email`) with a red `*` on every label; **Tajawal `@font-face` metric override ported from admin to the public `app.css`** (self-hosted 400/500/600/700/800 incl. a fetched `Tajawal-ExtraBold.ttf`; dropped Tajawal from the Google `@import`; removed the old `.lfc-season`/`.lfc-section-note` padding nudges) — see [[tajawal-metric-ride-high]].
- **T14 (2026-07-02) review — APPROVED & merged (`c4e4e21`, ff to main; 163 tests green, `migrate:fresh --seed` clean).** Branch `task/T14-registration-derive-year-country-selects`. Public form drops `year_of_birth` entirely (input + `rules()` + `attributes()` + the `withValidator()` birth-year cross-check + the now-dead `validation.birth_year_mismatch` key EN **and** AR); **derived server-side** in `PublicRegistrationService::create()` via `Carbon::parse($data['date_of_birth'])->year`, so the NOT-NULL `year_of_birth` column stays populated (test asserts `=== 2014` for dob `2014-05-10`). `country_of_birth`/`citizenship` are now `<select>`s sourced from new `config/countries.php` (`country ⇒ nationality`, GCC-first + 97 entries incl. `Qatar ⇒ Qatari`) via `App\Support\Countries::countries()/nationalities()` — **keys = the canonical stored strings**, so legacy `Qatar`/`Qatari` remain valid; `Rule::in(array_keys(...))` replaces `LatinText` on **only** those two, LatinText intact on `full_name`/`school`/`previous_club`/`parent_name` (**T13 preserved**). Controller DRYs `positionOptions()`; blade mirrors the `playing_position` select markup (`old()` selected, disabled placeholder, `dir="ltr"`, latin-JS attrs dropped). **PM independently verified:** 163 tests / 520 assertions green, fresh migrate+seed clean, EN/AR parity 54/54, whole-repo grep for the removed keys = none, `CandidateDataNormalizer` only touches `consent_at` (doesn't mangle the values or derived year). New tests cover no-year-input/select-render/derived-year/invalid-country+nationality-rejected.
  - **Minor (non-blocking):** `Countries::countries()/nationalities()` rebuild the collection on each of ~4 calls per request (negligible for a public form); the AR form shows **English** country labels by design (`dir="ltr"`, per the brief — AR country localization was explicitly out of scope). Merged locally; **not pushed** (main is ahead of origin — push at will).
  - **Superseded by T15:** `config/countries.php` + `App\Support\Countries` were replaced by `App\Enums\Country`/`Nationality`; the admin-form parity landed in the combined T15+T14b branch (see next note).
- **T15 + T14b (2026-07-02) review — APPROVED & merged together (`016a4ab`, ff to main; 165 tests / 527 assertions green, `migrate:fresh --seed` clean).** Combined branch `task/T15-country-nationality-enums`. Two string-backed enums `App\Enums\Country` (`Qatar='Qatar'`…) + `App\Enums\Nationality` (`Qatari='Qatari'`…), `HasLabel` → `enums.{countries,nationalities}.<CaseName>`, `flag()` emoji (Nationality delegates to its country's flag), `options()` = `[value => "🇶🇦 Qatar"]`. `enums.php` +97 countries +97 nationalities each locale (EN + MSA), **242/242 parity**. Public form: `Rule::enum(Country|Nationality)` + enum `options()`, dropped `dir="ltr" lang="en"` so AR renders RTL. Admin `CandidateForm`: searchable `Select`s with legacy-value tolerance (`Enum::options() + [record's current value]`), `year_of_birth` input removed. Year-of-birth **centralized in `CandidateDataNormalizer`** (`!empty(date_of_birth)` guard) → redundant line dropped from `PublicRegistrationService`; covered on create + edit. `LatinText` kept on `full_name`/`school`/`previous_club`/`parent_name` (**T13 intact**), removed from only country/citizenship. `config/countries.php` + `App\Support\Countries` deleted, no dangling refs.
  - **PM independently verified (runtime bootstrap, not just tests):** enum values are a **1:1 match with T14's deleted config — 97/97 countries + 97/97 nationalities, none missing/extra → existing rows stay valid, NO migration** (the point of the two-enum decision); **`flag()` is exhaustive** on every case (no lurking `UnhandledMatchError`); no duplicate demonym values; every enum case name has a key in both locales; `options()` renders `🇶🇦 قطر` / `🇶🇦 قطري` under `ar`. Then ran the full suite (165 green) + fresh seed clean myself.
  - **One deliberate, correct deviation from the brief:** I'd specced `Rule::enum` on the **admin** form too, but the coder omitted it there and relies on Filament `Select`'s option-constraint — **the right call**, because a strict `Rule::enum` would reject exactly the legacy out-of-enum values the tolerance is meant to let through on edit. Kept it on the public form (no legacy-tolerance need there).
  - **Minor (non-blocking):** `Country/Nationality::options()` rebuild their arrays per call (negligible); AR admin/infolist *display* of country still shows the stored English string (localized view display was explicitly out of scope — optional later follow-up). **Merged locally; not pushed** (main ahead of origin).
- **T16 (2026-07-02) review — APPROVED (code-complete); `DONE*` = on-device Android/dark walkthrough still pending. On branch `task/T16-mobile-monorepo-core`, NOT yet merged to `main`.** Two commits: `61359a7` (restructure) + `3d49f7a` (parity fix). `mobile/` is now a pub workspace — `packages/lfc_core` (shared 90%) + `apps/demo_app_{one,two,three}` thin shells. **Seam:** `brandProvider` throws unless overridden and supplies `Brand{appTitle,palette,wordmark,logo,heroLogo}`; `appConfigProvider` overridden per app with `AppConfig.fromEnvironment(defaultBaseUrl:)` (retains `--dart-define=API_BASE_URL` + Android `10.0.2.2`/iOS `localhost` default); each app's `app.dart` wraps `CoreApp` in a `ProviderScope`. `brand_mark.dart` + the theme builders now read the injected brand. l10n/tests/flags moved into `lfc_core` (flags referenced with `package:'lfc_core'`, verified); **fonts duplicated per-app** (the brief's accepted fallback — package-qualified font decls didn't build). Native IDs split correctly on **both** platforms incl. MainActivity path/`package` + `.RunnerTests`. Coder ran: `flutter analyze` + `flutter test` green in core + all 3 apps; `php artisan test` 165 green (unchanged); `flutter run` demo_app_one OK on iOS sim only.
  - **DEFECT — caught in review, FIXED in `3d49f7a`.** The initial `BrandPalette` collapsed the Lusail navy scale into 4 tokens and **dropped `navy600` + `navy500`**, so demo_app_one (which must equal pre-refactor `main`) silently regressed 3 colours: **dark `ColorScheme.primary`** `#2E659B`→`#113F71` (worst — low-contrast in dark mode), light `ColorScheme.tertiary` and the light hero-gradient end stop both `#1C5288`→`#0B3059`. Fix added `primaryMid`(`#1C5288`=navy600) + `primaryOnDark`(`#2E659B`=navy500) to `BrandPalette` (+ `.lusail` and the 2 placeholder palettes) and repointed the 3 mappings. **PM re-verified hex-for-hex against the original consts — parity now exact**; only those 3 theme lines + 2 palette fields changed, no collateral drift.
  - **Open (why `DONE*`):** the **on-device visual walkthrough was never run across the whole T16 effort** — no Android `flutter run` at all, no dark-mode A/B. Colour parity is now provable on paper, but Android-specific things a diff can't confirm (duplicated-font rendering, RTL, flags/logo load, camera scanner) remain unchecked. Do once before demo: demo_app_one full login→scanner in **EN+AR, light+dark, on an Android emulator**; confirm demo_app_two installs alongside it (distinct icon/id). *Process (recurring): agent did iOS-only + skipped the manual pass — the exact gap that let the colour regression through initially.*
- **⚠️ TODO — restore the Consent section (TEMP hidden 2026-07-02, user request).** The consent `<div class="lfc-form-section">` is Blade-commented out in `resources/views/public/register.blade.php` (search `TEMP: Consent section hidden`), and `StorePublicRegistrationRequest::prepareForValidation()` auto-fills `consent_given => '1'` to satisfy the still-present `accepted` rule (consent is also hardcoded `true` in `PublicRegistrationService::create()`, so records stay compliant). **To restore:** (1) un-comment the consent block in the view; (2) delete the `prepareForValidation()` shim from the request; (3) revert test `test_consent_is_auto_applied_while_section_is_hidden` back to asserting consent is **required** (was `test_consent_is_required`). Nothing else depends on the shim.
- **T17 (2026-07-05) review — APPROVED & merged (`5635cc9`, ff to main; 169 tests / 557 assertions green, verified here on a fresh run).** Branch `task/T17-app-scoping`, cut from `main` (`87adb28`). App-scoping spine, backend-only, app-one behaviour unchanged. `App\Enums\AppKey` (`app_one`/`app_two`) + `LedgerUnit` (`points`/`discount_pct`, a type-safe extra the coder added) + `AccountType` gains `Member`/`VvipMember`; migration adds indexed `app` (default+backfill `app_one`) to `parent_accounts`/`fixtures`/`offers` and `unit` (default `points`) to `point_transactions`. `App\Support\AppContext` (singleton) + `App\Models\Concerns\ScopedToApp` (global scope keyed off `AppContext::current()`, **inert when null** — this is what keeps the existing suite green; `qualifyColumn` so it's join-safe) applied to the 3 models with `forApp()` + `withoutAppScope()`; `unit` cast on `PointTransaction`; `ParentAccountFactory` defaults `app_one`. `enums.php` EN/AR parity for the 3 new key groups. `AppScopingTest` proves backfill, inert-no-context (`Model::count() == raw count`), scoped isolation both directions, and bypass; `EnumLabelTranslationTest` extended EN+AR. Append-only ledger untouched (`unit` set at insert only). Handoff clean (1 commit, branch, committed before handoff — good).
  - **Heads-ups for downstream (non-blocking; NOT defects):** (1) **`ScopedToApp` scopes READS only — it does not auto-stamp `app` on create.** T19/T20/T21 write paths MUST set `app` explicitly (or from `AppContext`) on every new app-two row, else DB-default `app_one` silently mis-tags them. Consider an opt-in `creating` auto-stamp in T18/T19 if it proves error-prone. (2) `AppContext` is a **container singleton** — request-scoped under `php artisan serve` (fine for the demo), but whoever SETS it (T18 panel middleware, T20 `X-App-Key` middleware) must set/reset per request; the test's `tearDown()->clear()` is the right pattern to mirror. (3) `scopeForApp()` does not strip the global scope, so with a context set, `forApp(<other app>)` yields an impossible `app=X AND app=Y` → empty; intended use is context-less or `withoutAppScope()->forApp(...)` (as the test does) — just a footgun to document for T18+.
- **T18 (2026-07-05) review — APPROVED & merged (`cf49a7b`, ff to main; 173 tests / 561 assertions green, verified here; `migrate:fresh --seed` clean).** Branch `task/T18-admin-panels-split`, cut from `main` (`7b2180a`). Clean split, low-churn approach honoured — **app-one resources/widgets NOT moved**, so its diff is just id/path/middleware. Old `AdminPanelProvider` → renamed to the `AppliesLusailBrand` trait (git rename, brand block byte-identical: colours ramp, logo, favicon, Tajawal+Changa, dark default, both render hooks incl. shared `filament.admin.locale-switcher`). `AdminAppOnePanelProvider` (id/path `admin-app-one`, **stays `->default()`** so `Livewire::test` resource tests + Shield discovery are untouched — they passed unmodified) + `AdminAppTwoPanelProvider` (id/path `admin-app-two`, own empty `Filament/AppTwo/{Resources,Pages,Widgets}` discovery dirs w/ `.gitkeep`, only `AccountWidget`). `SetAppContext` middleware (parametrized `:app_one`/`:app_two`, `AppKey::from()`) wired per panel — app-one context is inert today (all rows `app_one`) but auto-isolates once app-two data exists. `User::canAccessPanel()` gates `admin-app-two` to `Admin` only (app-one stays Admin/Coach/Management). `routes/web.php`: locale fallback + candidate-doc path → `/admin-app-one` (route **names** stable), added `Route::redirect('/admin','/admin-app-one')`. `bootstrap/providers.php` registers both, old provider deleted. Tests: paths updated + app-two access (Admin 200, login 200) + denial (Coach/Management 403). `DEMO-WALKTHROUGH` admin URL updated. Handoff clean (1 commit, branched from current main, committed before handoff — good). **Admin URL moved `/admin` → `/admin-app-one`** (bookmarks/demo scripts).
  - **Optional pre-demo check (not blocking):** branding didn't change (byte-identical trait) + app-one discovery unchanged, so a browser render pass is low-value; if desired, eyeball `/admin-app-one` (full nav) + `/admin-app-two` (empty nav, same Lusail look) logged in as `admin@lfc.test`, EN+AR.
  - **For T19 (Shield):** app-two's first resources will need `php artisan shield:generate` so policies/permissions exist; the `Admin` super-role bypasses via the `before` gate regardless, so the demo admin sees them either way. — **⚠️ THIS WAS WRONG (proven in T19): do NOT run `shield:generate`. See T19 review.**
- **⚠️ AUTHZ INVARIANT (learned in T19, applies to T20/T21/all future Filament resources):** this app authorizes **entirely** via `User::canAccessPanel()` + the **absence of model policies** — every app-one/app-two resource default-allows (Filament's behaviour when no policy exists). `config/filament-shield.php` has `super_admin.define_via_gate => false` **and the seeder assigns ZERO permissions** to any role (`DatabaseSeeder` just creates Admin/Coach/Management + `syncRoles(['Admin'])`). Consequence: introducing **any** model policy (e.g. via `php artisan shield:generate`) without also seeding+assigning its permissions **locks out EVERYONE, including the demo Admin** (empirically 200→403). **So: never run `shield:generate`; never add a model Policy** for new resources. Access is controlled by which panel a resource lives in (+ `canAccessPanel`). The only policy in the repo is `RolePolicy` (Shield's own role management).
- **T19 (2026-07-05) review — APPROVED with a folded-in fix; merged (`a4fc49f`, ff to main; 180 tests / 596 assertions green, verified here; `migrate:fresh --seed` clean).** Branch `task/T19-members-discount` (coder `38a03fd` + my review fix `a4fc49f`). App-two "normal member": `POST /api/v1/auth/register` (open self-signup → `app=app_two`+`account_type=member`+`accepted_at`, returns Sanctum token; **`app` stamped explicitly** per the T17 write-path rule); accrual as **config constants** (`config/loyalty.php`: `discount_bp_per_attendance=50` = 0.5%, `discount_cap_bp=1000` = 10%) stored in `point_transactions.points` as percent-basis-points with `unit=discount_pct` (NOT a PointRule — `resolveRule` is fixture-team-scoped; deliberate deviation, app one untouched). `ScanController::scan()` branches on `$parent->isMember()`: creates the `AttendanceScan` (same `unique(parent,fixture)` dedupe→409) and appends `+min(50, cap−balance)` via new `PointsEngine::creditAttendanceDiscount()` — **app-one linked-player path verbatim**. `ParentAccount::pointsBalance()` now filters `unit=points` (app-one all-points → unchanged) + new `discountBalance()`/`discountPercent()`/`isMember()`. `ParentAccountResource` (+`/me`) exposes `app`, `discount_percent`, `discount_cap_percent`; `ProfileController` gained a `instanceof ParentAccount` guard (good defensive add — a staff token hitting `/me` would've fataled on `pointsBalance()`). App-two admin `MemberResource` (view-only, `getEloquentQuery()` → `account_type=member`, panel auto-scopes to app_two) + read-only `DiscountTransactionsRelationManager`. EN/AR labels (parity). Solid `AppTwoMemberTest` (register/accrual/dedupe/cap/balance-separation/app-one-regression/admin-scoping).
  - **Regression caught & fixed during review (root cause = my brief's `shield:generate` instruction):** `shield:generate` auto-created `app/Policies/ParentAccountPolicy.php` for the pre-existing `ParentAccount` model (which backs app-one's `ParentAccounts` resource). Per the AUTHZ INVARIANT above, that flipped app-one ParentAccounts access **200→403 for BOTH Admin and Coach** (verified by A/B against `main` in an identical harness). Fix = **deleted the policy** (+ reverted a cosmetic `RolePolicy` blank line); app-two Members needs no policy (panel is Admin-only via `canAccessPanel`). Net diff on `main` contains no `ParentAccountPolicy`. My earlier "Admin bypasses regardless" claim was empirically false here (`define_via_gate=false` + no seeded perms).
- **T20 (2026-07-05) review — APPROVED & merged (`7314334`, ff to main; 186 tests / 638 assertions green, verified here; `migrate:fresh --seed` clean).** Branch `task/T20-content-cms`, cut from `main` (`b66fe4e`). Clean; internalised both prior lessons (**no policy, no `shield:generate`** — verified only `RolePolicy` on branch). **`ScopedToApp` now auto-stamps `app`** on `creating` from `AppContext` (only when a context is set → app-one panel stamps `app_one` = existing default, no change; tested both directions) — kills the T19 write-path footgun. **`fixtures` extended** (not a new table, so browsed match = scanned match): `team_id` nullable via drop-FK→change→re-add-cascade (T7b pattern), + `competition`/`is_home`/`our_score`/`opponent_score`; `Fixture::isPlayed()`/`isUpcoming()`; app-one `FixtureResource` + scan path untouched. New `news_posts` + `standings` tables (bilingual via `HasLocalizedContent`, `ScopedToApp`, `app` default `app_one`). Guest (no-auth) `/api/v1/content/{news,news/{id},fixtures,results,standings}` inside the `v1` group, wrapped in new `SetAppContextFromHeader` (`X-App-Key` → `AppKey::tryFrom` else `app_two`); payloads localised via `->localized()` + `api.locale`; news `show` 404s on unpublished; standings position derived by `points→GD→GF` ordering. App-two `NewsResource`/`MatchResource`/`StandingResource` (bilingual, nav group `content`). Bonus hardening: `SetAppContext` now clears context in a `finally` (addresses the per-request heads-up). Solid `AppTwoContentApiTest` (9) + auto-stamp test in `AppScopingTest`. Handoff clean (1 commit, pint --dirty, committed before handoff — good).
  - **Minor, non-blocking (logged only):** guest `/content/fixtures` honours `X-App-Key`, so `X-App-Key: app_one` would return app-one academy fixture *schedules* publicly (no PII — match times only; fine for demo). If these ever go to prod, pin the content endpoints to `app_two` or authenticate.
- **T22 (2026-07-05) review — APPROVED & merged (`42408a9`, ff to main; verified here: `flutter analyze` clean + `flutter test` green in lfc_core (25), demo_app_two (smoke), AND demo_app_one (app-one regression clean); `php artisan test` untouched).** Branch `task/T22-app-two-shell`, cut from `main` (`bc79df3`). First app-two **mobile** task, additive. `demo_app_two` rebranded to Lusail (`BrandPalette.lusail` + crest, wordmark `LUSAIL`/`SUPPORTERS`, appTitle `Lusail SC`; native labels updated) and now renders new **`SupporterApp`** (a standalone root in `lfc_core` mirroring `CoreApp` — `CoreApp` untouched). `AppConfig` gained nullable `appKey`; `dioProvider`+`staffDioProvider` send `X-App-Key` **only when set** (app one leaves it null → unchanged — verified). New `lfc_core` **content feature**: `NewsSummary`/`NewsArticle`/`MatchSummary`/`StandingRow` models + `ContentRepository` (unwraps `{data}`) + `news/fixtures/results/standings/newsArticle` FutureProviders, all reading the guest `/content/*` API. **`SupporterShell`** = `BrandAppBar` + bottom nav (Home/Matches/Membership): Home = news list + article detail; Matches = Fixtures/Results/Table (standings highlight own club); Membership = **guest sign-in gate** (T23 stub) / authed placeholder — per the locked UX (full nav + prompt; single role-driven authed shell). EN/AR l10n added (regenerated `AppLocalizations`). Tests (all run here): X-App-Key header, guest home news render, standings table + own-club highlight, guest membership gate, **RTL** (Directionality.rtl via AR nav label), smoke. **Note:** the app-one file changes in the diff (players screens, app_theme, brand_app_bar, pills, test_brand) are **pure `dart format` reflow — no semantics** (verified). On-device `flutter run` pass still pending (user).
- **T21 (2026-07-05) review — APPROVED & merged (`bc79df3`, ff to main; 191 tests / 684 assertions green, verified here; `migrate:fresh --seed` clean). LAST app-two backend task — the whole app-two backend + admin is now complete.** Branch `task/T21-vvip-membership`, cut from `main` (`27dc516`). No policy / no `shield:generate` (verified only `RolePolicy`). New `membership_tiers` (bilingual, `ScopedToApp`, `level`/`accent_color`/`is_active`) + `membership_benefits` (belongs to tier, cascade-delete, bilingual, `sort_order`); `parent_accounts` gains `membership_tier_id` (nullOnDelete) + `member_number` + `membership_valid_until`. `ParentAccount::isVvipMember()` + `membershipTier()`. **`/offers` cross-app leak FIXED** — `OfferController` now `->forApp($parent->app)->visibleTo($parent)` (app one unchanged: its accounts+offers are all `app_one`); regression test covers app-two vvip (VVIP+All, no app-one offers), app-two member (All only), app-one parent (app-one only). New `GET /me/benefits` (`BenefitController` single-action, `instanceof` guard, `{data:null}` when no tier, localised tier+ordered benefits) + `/me` compact `membership` block. App-two Filament: `MembershipTierResource` + `BenefitsRelationManager` (CRUD), `VvipMemberResource` (admin-creates vvip_member via `mutateFormDataBeforeCreate` → `account_type=vvip_member`+`is_vvip=true`+`accepted_at`; `app` auto-stamped by panel context; `getEloquentQuery` filters `account_type` + relies on the panel's `AppContext` for app), `OfferResource` (bilingual, mirrors app-one). `AppTwoVvipMembershipTest` (5): vvip create + query scoping, `/me/benefits` localised/null, `/me` card, offers isolation, tier/benefit cascade — its `tearDown` clears `AppContext` + `Filament::setCurrentPanel(null)`. Handoff clean (1 commit, committed before handoff, factories added).
