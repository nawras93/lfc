// Leave empty so AppConfig.fromEnvironment picks the platform-aware host
// (Android emulator -> 10.0.2.2, else localhost). Override at build time with
// --dart-define=API_BASE_URL=http://<host>:8000/api/v1 (e.g. a LAN IP for a
// physical device).
const demoTwoApiBaseUrl = '';
